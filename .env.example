# ═══════════════════════════════════════════════════════════
#  AEGIS PROTOCOL V4 — Environment Variables
#  Copy to .env and fill in your values
#  NEVER commit .env — it is in .gitignore
# ═══════════════════════════════════════════════════════════

# ── Wallet ────────────────────────────────────────────────
# Owner / deployer private key (funded on Tenderly VNet)
PRIVATE_KEY=0x...

# Owner wallet address (used by frontend + scripts)
DEV_WALLET_ADDRESS=0x...

# ── Tenderly VNet (auto-filled by new_tenderly_testnet.ps1) ─
TENDERLY_RPC_URL=https://virtual.base.eu.rpc.tenderly.co/<UUID>
TENDERLY_TESTNET_UUID=<UUID>
TENDERLY_KEY=<your-tenderly-api-key>

# ── Deployed Contracts (auto-filled by new_tenderly_testnet.ps1) ─
AEGIS_MODULE_ADDRESS=0x...
KEYSTONE_FORWARDER=0x...

# ── Chainlink CRE Node ────────────────────────────────────
# CRE node signing key (funded on VNet, used inside Docker)
CRE_ETH_PRIVATE_KEY=0x...

# ── Oracle APIs (used via ConfidentialHTTPClient inside DON) ─
# These keys NEVER leave the DON in production
BASESCAN_API_KEY=<your-basescan-api-key>
OPENAI_API_KEY=sk-...
GROQ_API_KEY=<your-groq-api-key>

# ── GoPlus Security API ────────────────────────────────────
GOPLUS_APP_KEY=<your-goplus-app-key>
GOPLUS_APP_SECRET=<your-goplus-app-secret>

# ── Frontend ──────────────────────────────────────────────
# For the frontend Oracle Feed + token resolution
COINGECKO_API_KEY=<your-coingecko-api-key>

# ElevenLabs TTS (optional — for voice narration in UI)
ELEVEN_LABS_KEY=<your-elevenlabs-key>

# Pimlico bundler (for ERC-4337 UserOp relay)
PIMLICO_API_KEY=<your-pimlico-api-key>

# ── Legacy (unused in V4, kept for backward compat) ───────
# AEGIS_VAULT_ADDRESS=   # V3 AegisVault — replaced by AEGIS_MODULE_ADDRESS