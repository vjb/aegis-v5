# ═══════════════════════════════════════════════════════════
#  AEGIS PROTOCOL V5 — Environment Variables
#  Copy to .env and fill in your values
#  NEVER commit .env — it is in .gitignore
# ═══════════════════════════════════════════════════════════

# ── Wallet ────────────────────────────────────────────────
# Owner / deployer private key (funded on Base Sepolia)
PRIVATE_KEY=0x...

# Owner wallet address (used by frontend + scripts)
DEV_WALLET_ADDRESS=0x...

# ── Base Sepolia RPC ────────────────────────────────────
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# ── Deployed Contracts (after running DeployMocks.s.sol) ───
AEGIS_MODULE_ADDRESS=0x...

# ── Chainlink CRE Node ────────────────────────────────────
# CRE node signing key (funded on Base Sepolia, used inside Docker)
CRE_ETH_PRIVATE_KEY=0x...

# ── Oracle APIs (used via ConfidentialHTTPClient inside DON) ─
# These keys NEVER leave the DON in production
BASESCAN_API_KEY=<your-basescan-api-key>
OPENAI_API_KEY=sk-...
GROQ_API_KEY=<your-groq-api-key>

# ── GoPlus Security API ────────────────────────────────────
GOPLUS_APP_KEY=<your-goplus-app-key>
GOPLUS_APP_SECRET=<your-goplus-app-secret>

# ── Frontend ──────────────────────────────────────────────
# For the frontend Oracle Feed + token resolution
COINGECKO_API_KEY=<your-coingecko-api-key>

# ElevenLabs TTS (optional — for voice narration in UI)
ELEVEN_LABS_KEY=<your-elevenlabs-key>

# Pimlico bundler (for ERC-4337 UserOp relay)
PIMLICO_API_KEY=<your-pimlico-api-key>

# ── Legacy (unused in V5, kept for backward compat) ───────
# AEGIS_VAULT_ADDRESS=   # V3 AegisVault — replaced by AEGIS_MODULE_ADDRESS

# ── V5: Account Abstraction (ERC-4337 / ERC-7579 / ERC-7715) ──────────────
# AI Agent wallet (gas-only — trading capital stays in Safe)
AGENT_PRIVATE_KEY=0x...
AGENT_WALLET_ADDRESS=0x...

# Safe Smart Account (populated by scripts/v5_setup_safe.ts in Phase 2)
SAFE_ADDRESS=0x0000000000000000000000000000000000000000

# Local alto bundler (docker-compose --profile v5) — used in Phases 2-4
BUNDLER_RPC_URL=http://localhost:4337

# Pimlico hosted bundler URL — Phase 5 only (Base Sepolia)
# Format: https://api.pimlico.io/v2/base-sepolia/rpc?apikey=<PIMLICO_API_KEY>
PIMLICO_BUNDLER_URL=

# ERC-4337 EntryPoint v0.7 — canonical, deployed on Base and Tenderly Base fork
ENTRYPOINT_ADDRESS=0x0000000071727De22E5E9d8BAf0edAc6f37da032

# Rhinestone SmartSessionValidator — installed on Safe in Phase 3
# Address populated after Phase 2 Safe deployment + module install
SMART_SESSION_VALIDATOR_ADDRESS=0x0000000000000000000000000000000000000000
